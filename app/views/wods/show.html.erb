<% @wods.each do |wod| %>
  <div class="card wod-detail-card">
    <div class="card-header">
      <%= wod.title %>
    </div>
    <div class="card-body">
      <%= wod.desc_html %>
    </div>
  </div>
<% end %>